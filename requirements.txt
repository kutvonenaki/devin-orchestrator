# Single requirements file — runtime + test deps (image self-tests).
# Pinned for reproducibility. T2 runs `pip install -r requirements.txt` into
# the `devin-takehome` conda env, then `pip freeze` confirms/locks exact
# versions before Docker is built.

# --- runtime ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- tests (mocked; no network, no credentials) ---
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-httpx==0.35.0
anyio==4.8.0
